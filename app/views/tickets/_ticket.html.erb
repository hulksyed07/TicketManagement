<br>
<%= ticket.incident %> 
<%= ticket.dou %> 
<%= get_engineer_name(ticket.engineer_id)%>
<%= get_status_name(ticket.status_id)%>
<%= link_to 'Show', ticket %>
<%= link_to 'Edit', edit_ticket_path(ticket) %>
<%= link_to 'Destroy', ticket, method: :delete, data: { confirm: 'Are you sure?' } %>