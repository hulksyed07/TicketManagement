<%= form_for(@ticket, remote: true) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :incident %><br />
      <%= f.text_field :incident %>
  </div>
  <div class="field">
      <%= f.label :Last_Update %><br />
      <%= f.date_select :dou %>
  </div>

  <div class="field">
      <%= f.label :engineer %><br />
      <%= f.collection_select :engineer_id, @engineers, :id, :name, include_blank: false %>
  </div>
  <div class="field">
      <%= f.label :application %><br />
      <%= f.collection_select :application_id, @applications, :id, :name, include_blank: false %>
  </div>
    <div class="field">
      <%= f.label :status %><br />
      <%= f.collection_select :status_id, @statuses, :id, :name, include_blank: false %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
